<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>UsageByModelJson__c</fullName>
    <label>Usage by Model (JSON)</label>
    <description>Running per-model totals for the whole session: [{model, calls, tokensIn, tokensOut, cacheRead}], merged from each turn's ChatMessage__c.UsageJson__c. Held at session level so usage-by-model reporting needs only the session list, not a scan of every message.</description>
    <type>LongTextArea</type>
    <length>32768</length>
    <visibleLines>3</visibleLines>
</CustomField>
